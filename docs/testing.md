# Testing

## Overview

Testing strategy for future unit, integration, mock Roku, mock provider, planner, and database checks.

## Responsibilities

This document tracks the responsibilities assigned to this subsystem by the architecture phase. It intentionally does not define implementation details before the relevant phase begins.

## TODO

- Add test fixtures only when implementation exists.
- Keep tests deterministic and hardware-independent by default.
- Expand CI checks as each project phase adds runnable code.
